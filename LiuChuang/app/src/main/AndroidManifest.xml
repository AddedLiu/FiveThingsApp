<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.studyjamscn.s1.sj23.liuchuang">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TextSize"
            android:label="@string/title_activity_text_size" />
        <activity
            android:name=".Torch"
            android:label="@string/title_activity_torch" />
        <activity
            android:name=".Mobile_network"
            android:label="@string/title_activity_mobile_network" />
        <activity
            android:name=".WLAN"
            android:label="@string/title_activity_wlan" />
        <activity
            android:name=".Contacts"
            android:label="@string/title_activity_contacts"></activity>
    </application>

</manifest>
